rootProject.name = "todolist"

pluginManagement {
    repositories {
        maven("https://artifactory.tcsbank.ru/artifactory/maven-all")
    }
}

buildscript {
    repositories {
        maven("https://artifactory.tcsbank.ru/artifactory/maven-all")
    }
}
